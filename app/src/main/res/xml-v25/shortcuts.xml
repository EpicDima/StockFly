<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:enabled="true"
        android:icon="@drawable/ic_search"
        android:shortcutDisabledMessage="@string/shortcut_search_disabled_label"
        android:shortcutId="search"
        android:shortcutLongLabel="@string/shortcut_search_long_label"
        android:shortcutShortLabel="@string/shortcut_search_short_label">

        <intent
            android:action="android.intent.action.VIEW"
            android:targetClass="ru.yandex.stockfly.ui.MainActivity"
            android:targetPackage="ru.yandex.stockfly">

            <!-- from MainActivity -->
            <extra android:name="open_fragment" android:value="open_search_fragment" />

        </intent>

    </shortcut>

</shortcuts>